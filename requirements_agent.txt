# Swarm-Ev2 Agent 容器依赖
# 仅包含 mlebench-env 基础镜像中缺少的包
# 基础镜像已有：PyTorch, TensorFlow, sklearn, pandas, numpy, scipy, Flask 等

# 配置管理
omegaconf>=2.3.0
dataclasses-json>=0.6.0
python-dotenv>=1.0.0

# LLM 客户端
openai>=1.0.0
anthropic>=0.20.0
backoff>=2.2.0

# 工具库
pydantic>=2.0.0
pydantic-settings>=2.0.0
rich>=13.0.0
humanize>=4.0.0
genson>=1.2.0
jinja2>=3.1.0

# Embedding 模型
huggingface_hub>=0.20.0
sentence-transformers>=2.2.0
